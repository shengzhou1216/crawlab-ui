interface LLayouts {
  components: {
    header: {
      logout: string;
    };
  };
  detailLayout: {
    navTabs: {
      toggle: {
        tooltip: {
          expand: string;
          collapse: string;
        };
      };
    };
  };
}
