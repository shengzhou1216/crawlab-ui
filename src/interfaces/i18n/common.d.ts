interface LCommon {
  control: {
    enabled: string;
    disabled: string;
  };
  actions: {
    view: string;
    delete: string;
  };
  messageBox: {
    message: {
      delete: string;
    };
  };
}
